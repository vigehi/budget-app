<%= form_with(model: entity) do |form| %>
  <% if entity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entity.errors.count, "error") %> prohibited this entity from being saved:</h2>

      <ul>
        <% entity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.number_field :id,  placeholder: 'ID', class:"field-login" %>
  </div>

  <div>
    <%= form.text_field :name,  placeholder: 'Name', class:"field-login" %>
  </div>

  <div>
    <%= form.text_field :amount,  placeholder: 'Amount', class:"field-login"%>
  </div>

  <div>
    <%= form.submit "submit", class:"btn btn-primary my-btn2" %>
  </div>
<% end %>
